# Supabase Connection Flow

## Quick Decision Tree

```
Do you want to connect to Supabase?
│
├─ YES, Local Development (Recommended)
│  │
│  ├─ Have Docker installed?
│  │  ├─ YES → Run: npm run setup:supabase ✅
│  │  └─ NO → Install Docker → npm run setup:supabase ✅
│  │
│  └─ Result: ✅ Local Supabase running on your machine
│
└─ YES, Remote/Production
   │
   ├─ Have Supabase account?
   │  ├─ YES → Get credentials from dashboard
   │  └─ NO → Create account at supabase.com
   │
   ├─ Update .env.local with credentials
   ├─ Run migrations in SQL Editor
   └─ npm run dev ✅
```

## Setup Flow Diagram

### Automated Local Setup (Recommended)

```
┌─────────────────────────────────────────┐
│  npm run setup:supabase                 │
└────────────────┬────────────────────────┘
                 │
                 ▼
┌─────────────────────────────────────────┐
│  1. Check Docker is running             │
│     ├─ Running? ✅ Continue             │
│     └─ Not running? ❌ Show error       │
└────────────────┬────────────────────────┘
                 │
                 ▼
┌─────────────────────────────────────────┐
│  2. Install Supabase CLI                │
│     npm install supabase --save-dev     │
└────────────────┬────────────────────────┘
                 │
                 ▼
┌─────────────────────────────────────────┐
│  3. Start Local Supabase                │
│     npx supabase start                  │
│     (Downloads Docker images: ~5-10min) │
└────────────────┬────────────────────────┘
                 │
                 ▼
┌─────────────────────────────────────────┐
│  4. Update .env.local                   │
│     - NEXT_PUBLIC_SUPABASE_URL          │
│     - NEXT_PUBLIC_SUPABASE_ANON_KEY     │
│     - SUPABASE_SERVICE_ROLE_KEY         │
└────────────────┬────────────────────────┘
                 │
                 ▼
┌─────────────────────────────────────────┐
│  5. Migrations run automatically ✅     │
└────────────────┬────────────────────────┘
                 │
                 ▼
┌─────────────────────────────────────────┐
│  ✅ DONE! Ready to develop              │
│                                         │
│  - Supabase: http://127.0.0.1:54323    │
│  - App: npm run dev                     │
│  - Test: /test-connection               │
└─────────────────────────────────────────┘
```

### Manual Remote Setup

```
┌─────────────────────────────────────────┐
│  1. Go to supabase.com                  │
│     - Sign up / Log in                  │
│     - Create new project                │
│     - Wait ~2 minutes                   │
└────────────────┬────────────────────────┘
                 │
                 ▼
┌─────────────────────────────────────────┐
│  2. Get Credentials                     │
│     Settings → API                      │
│     - Copy Project URL                  │
│     - Copy anon public key              │
│     - Copy service_role key             │
└────────────────┬────────────────────────┘
                 │
                 ▼
┌─────────────────────────────────────────┐
│  3. Update .env.local                   │
│     Paste your credentials:             │
│     - NEXT_PUBLIC_SUPABASE_URL=...      │
│     - NEXT_PUBLIC_SUPABASE_ANON_KEY=... │
│     - SUPABASE_SERVICE_ROLE_KEY=...     │
└────────────────┬────────────────────────┘
                 │
                 ▼
┌─────────────────────────────────────────┐
│  4. Run Migrations                      │
│     In Supabase SQL Editor:             │
│     - Open 001_initial_schema.sql       │
│     - Copy & paste → Run                │
│     - Open 002_add_onboarding.sql       │
│     - Copy & paste → Run                │
└────────────────┬────────────────────────┘
                 │
                 ▼
┌─────────────────────────────────────────┐
│  5. Start the App                       │
│     npm run dev                         │
└────────────────┬────────────────────────┘
                 │
                 ▼
┌─────────────────────────────────────────┐
│  ✅ DONE! Ready to develop              │
│                                         │
│  - App: http://localhost:3000           │
│  - Test: /test-connection               │
│  - Supabase Dashboard: supabase.com     │
└─────────────────────────────────────────┘
```

## Validation Flow

```
┌─────────────────────────────────────────┐
│  npm run validate:supabase              │
└────────────────┬────────────────────────┘
                 │
                 ▼
┌─────────────────────────────────────────┐
│  Check 1: .env.local exists?            │
│  ├─ YES ✅ → Continue                   │
│  └─ NO ❌ → Show error                  │
└────────────────┬────────────────────────┘
                 │
                 ▼
┌─────────────────────────────────────────┐
│  Check 2: All variables set?            │
│  ├─ NEXT_PUBLIC_SUPABASE_URL            │
│  ├─ NEXT_PUBLIC_SUPABASE_ANON_KEY       │
│  └─ SUPABASE_SERVICE_ROLE_KEY           │
└────────────────┬────────────────────────┘
                 │
                 ▼
┌─────────────────────────────────────────┐
│  Check 3: Valid format?                 │
│  ├─ URL starts with http(s)://          │
│  ├─ Keys look like JWT tokens           │
│  └─ No placeholder values               │
└────────────────┬────────────────────────┘
                 │
                 ▼
┌─────────────────────────────────────────┐
│  Result                                 │
│  ├─ All pass ✅ → Ready to develop!    │
│  └─ Issues ❌ → Show what's wrong       │
└─────────────────────────────────────────┘
```

## Architecture Overview

```
┌─────────────────────────────────────────────────────────────┐
│                    Your Readee App                          │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐     │
│  │   Client     │  │    Server    │  │   API Routes │     │
│  │  Components  │  │  Components  │  │              │     │
│  └──────┬───────┘  └──────┬───────┘  └──────┬───────┘     │
│         │                  │                  │             │
│         ▼                  ▼                  ▼             │
│  ┌──────────────────────────────────────────────────┐      │
│  │         Supabase Client Libraries                │      │
│  │  - supabaseBrowser() for client                  │      │
│  │  - createClient() for server                     │      │
│  └──────────────────┬───────────────────────────────┘      │
└─────────────────────┼───────────────────────────────────────┘
                      │
                      │ .env.local credentials
                      │
                      ▼
┌─────────────────────────────────────────────────────────────┐
│                     Supabase Instance                       │
│                                                             │
│  Local (Development):          Remote (Production):        │
│  ┌────────────────────┐        ┌────────────────────┐     │
│  │ Docker Container   │        │ Supabase Cloud     │     │
│  │ 127.0.0.1:54321   │        │ *.supabase.co      │     │
│  └────────────────────┘        └────────────────────┘     │
│                                                             │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐    │
│  │  PostgreSQL  │  │     Auth     │  │   Storage    │    │
│  │   Database   │  │   Service    │  │   Service    │    │
│  └──────────────┘  └──────────────┘  └──────────────┘    │
└─────────────────────────────────────────────────────────────┘
```

## File Structure

```
readee-app2.0/
│
├── Setup Scripts (Run these!)
│   ├── setup-supabase.js      ← Cross-platform wrapper
│   ├── setup-supabase.sh      ← Unix/Mac/Linux
│   ├── setup-supabase.bat     ← Windows
│   └── validate-supabase.js   ← Validation
│
├── Configuration
│   ├── .env.local            ← Your credentials (not in git)
│   ├── .env.example          ← Template with instructions
│   └── package.json          ← NPM scripts
│
├── Documentation
│   ├── QUICKSTART.md         ← Start here! (2 min)
│   ├── SUPABASE_CONNECTION_GUIDE.md  ← Full guide
│   ├── SUPABASE_SETUP_SUMMARY.md     ← This PR summary
│   ├── ENV_SETUP.md          ← Environment details
│   └── TROUBLESHOOTING.md    ← Problem solving
│
├── Supabase Integration (Already exists)
│   ├── lib/supabase/
│   │   ├── client.ts         ← Browser client
│   │   ├── server.ts         ← Server client
│   │   ├── admin.ts          ← Admin client
│   │   └── proxy.ts          ← Proxy helper
│   └── supabase/
│       ├── config.toml       ← Local Supabase config
│       └── migrations/       ← Database schema
│           ├── 001_initial_schema.sql
│           └── 002_add_onboarding_complete.sql
│
└── Application (Your code)
    └── app/
        ├── (protected)/      ← Protected routes
        ├── (public)/         ← Public routes
        ├── test-connection/  ← Connection tester
        └── ...
```

## Quick Reference

### Commands
```bash
npm run setup:supabase      # Automated setup (recommended)
npm run validate:supabase   # Check configuration
npm run supabase:start      # Start local Supabase
npm run supabase:stop       # Stop local Supabase
npm run supabase:status     # Check status
npm run dev                 # Start dev server
```

### URLs (Local Setup)
```
App:              http://localhost:3000
Test Connection:  http://localhost:3000/test-connection
Supabase Studio:  http://127.0.0.1:54323
API:              http://127.0.0.1:54321
```

### Environment Variables
```env
NEXT_PUBLIC_SUPABASE_URL          # Supabase API URL
NEXT_PUBLIC_SUPABASE_ANON_KEY     # Public/anon key
SUPABASE_SERVICE_ROLE_KEY         # Admin key (server-only)
```

---

**Ready to start?** Run `npm run setup:supabase` 🚀
